// Stub — Auth.js v5 entry point. Will export { auth, handlers, signIn, signOut } from NextAuth(...).
// Implementation lands in IA-2 (PrismaAdapter, Credentials provider, session strategy).
export {};
